import { useState } from 'react'; import API from '../api/api.js'; import { useAuth } from '../context/AuthContext.jsx'; import { useNavigate } from 'react-router-dom';
export default function Login(){ const [form,setForm]=useState({email:'',password:''}); const {login}=useAuth(); const nav=useNavigate(); const submit=async()=>{ const res=await API.post('/auth/login',form); login(res.data); nav('/'); }; return (<div style={{padding:'60px',textAlign:'center'}}><h1 className='neon'>HustleHub Funnels</h1><input placeholder='Email' onChange={e=>setForm({...form,email:e.target.value})}/><br/><input placeholder='Password' type='password' onChange={e=>setForm({...form,password:e.target.value})}/><br/><button onClick={submit}>Login</button></div>); }